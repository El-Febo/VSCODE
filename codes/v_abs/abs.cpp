#include<iostream>
using namespace std;


//stampare il valore assoluto di una differenza andando a modificare il bit di segno
int main(){
    int a, b;
    cin >> a >> b;

    

    // int res = ((a - b) > 0) ? (a - b) : (a - b)*(-1);
    // cout << res << "\n";


}